{
  "name": "gabriella-speariett-com",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "cd frontend && npm install && npm run build",
    "frontend": "npm run dev --prefix frontend",
    "backend": "cd backend && uvicorn main:app --reload"
  }
}
